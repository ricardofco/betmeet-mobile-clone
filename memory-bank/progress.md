# Progress

> **Agent note:** This is your long-term progress tracker. Update it whenever you complete a Bolt, close a phase, or reach a major milestone.

## Overall Status
- **Architecture pivot (2026-07-01):** backend hosting resolved (ADR-026) — a standalone `backend/` Node/Express service lives **inside this repo**, own Supabase project, `betmeet-clone` untouched/independent. **ADR-030 (2026-07-01) superseded the original phased plan**: from Bolt 7 onward, each bolt implements its own matching backend capabilities in its own Implement/Test stage (verified for real against the live DB, not deferred). See `memory-bank/bolts/bolt-backend-phase1/` and `memory-bank/bolts/bolt-7-pools-core/`.
- **Current Phase:** Construction — **Bolt 9 (Navigation, i18n & Design Retrofit) FULLY CLOSED (2026-07-06)** — Layer 1 green (599 tests, `tsc`/`lint` clean) and **Layer 2 confirmed by the user's own manual device retest of all 5 fixes** (tab-bar icons, button-text clipping, Pools/My Pools Spanish text, My Pools layout, Settings hamburger — all confirmed working). ADR-041's open question resolved (unification, confirmed by the user). Delivered the nested Drawer→Tabs→per-tab-stack shell (ADR-042), the structural back-button fix, full i18n wiring (ADR-044) with the confirmed unified locale-switch behavior, and a bounded, explicitly-scoped Tamagui retrofit (ADR-045, after a real probe found and fixed two genuine Tamagui/Rspack incompatibilities). A previously-unplanned native dependency (`react-native-reanimated`/`react-native-worklets`, `@react-navigation/drawer`'s peer dependency) was discovered mid-Implement, probed for real, and recorded as ADR-046. **The user's own manual Layer 2 (device) pass then found 5 further real defects, all fixed** (tab-bar icons missing → `lucide-react-native` chosen after `@tamagui/lucide-icons` was probed and rejected for pulling in a duplicate `@tamagui/core@1.x` engine, plus a Hermes-parser build bug that surfaced only via a real dev-server build, new ADR-047; clipped `PrimaryButton` text → a real `tamagui.config.ts` token-scale bug (`size`/`space` conflated) fixed at the root; hardcoded English in two in-scope `pools`-remote files, a genuine Implement-stage miss, now fixed; a cramped `my-pools-screen.tsx` layout, given real Tamagui-based polish; a missing hamburger button once inside Settings, fixed by sharing `renderHeaderMenuButton`). Full detail: `memory-bank/bolts/bolt-9-navigation-i18n-design-retrofit/implement-and-test.md §11`. **Bolt 10 (Scoring & rankings) is now unblocked and can open next.** Previously — **Bolt 8 (Pools advanced + predictions↔pools integration + account deletion) closed at Layer 1, fully — mobile (578 tests) AND backend, with backend verified REAL against the live Supabase DB** (a missing `backend/.env` earlier in the same session was flagged explicitly, then restored, then the full curl runbook was executed for real — see `memory-bank/bolts/bolt-8-pools-advanced-account-deletion/implement-and-test.md §3`). **The anti-bias-masking API-response-level check (the task brief's named headline risk) passed on all three branches; dual-save and account-deletion's atomicity held on both happy and failure paths — zero discrepancies found from the Model/Design/ADR expectations.** Delivered all six stories' mobile code (POOLS-3/6/7, PREDICTIONS-3/4, AUTH-6) and all matching backend handlers/services (ADR-030). Two prior ADRs (ADR-032, ADR-024) were formally re-examined per the task brief's instruction and both reconfirmed unchanged (ADR-036, ADR-037). New: ADR-038 (masking is server-only, with a permanent re-verification instruction), ADR-039 (dual-save/account-deletion atomicity via DB transaction, now proven for real), ADR-040 (ownership transfer's two-capability shape, now proven for real). **Bolt 9 (Scoring & rankings) is next.**
- **Bolts Completed:** 9 / 14 (plan grew from 13 to 14 bolts on 2026-07-03: **Bolt 9 — Navigation, i18n & Design Retrofit — inserted** after Bolt 8, per an approved mid-Construction change request; old Bolts 9–12 renumbered to 10–13, no scope change to any of them beyond an added Bolt-9 dependency. See `bolt-plan.md`, `audit.md`.) (Bolt 0 ✅, Bolt 1 ✅ both layers; Bolt 2 Layer 1 ✅ / Layer 2 pending manual; Bolt 3 Layer 1 ✅ / Layer 2 pending manual; Bolt 4 Layer 1 ✅ / Layer 2 pending manual; Bolt 5 Layer 1 ✅ / Layer 2 deferred to Bolt 6 (now addressed); Bolt 6 Layer 1 ✅ / Layer 2 pending manual; Bolt 7 Layer 1 ✅ / Layer 2 backend verification ✅ real, device paths pending manual; Bolt 8 Layer 1 ✅ mobile + backend (real) / Layer 2 device paths pending manual; **Bolt 9 Layer 1 ✅ (599 tests) / Layer 2 ✅ — 5 findings from the user's own manual pass fixed and retest-confirmed. Bolt 9 FULLY CLOSED.**)

## Milestones Achieved
- [x] Memory Bank and standards initialized
- [x] Domain knowledge enriched: `memory-bank/project/project-inventory.md`, `domain-overview.md`, `migration-analysis.md` (full analysis of `betmeet-clone`, the functional source of truth)
- [x] First intent captured (`/aidlc-inception`) — `memory-bank/intents/liga-mundial-mobile-migration/requirements.md` approved at Checkpoint 2
- [x] `system-context.md` drafted, incl. Module Federation host/remote topology
- [x] All 10 unit briefs + 47 stories drafted under `units/unit-01-auth/` … `units/unit-10-admin/`
- [x] Bolt plan drafted — `memory-bank/intents/liga-mundial-mobile-migration/bolt-plan.md` (13 bolts)
- [x] Checkpoint 3 (combined artifacts review) approved by user
- [x] Checkpoint 4 (Construction handoff) confirmed — Construction started on Bolt 0
- [x] Module Federation host/remote topology scaffolded — host (`rspack.config.mjs`) + one example remote `education` (`rspack.config.education-remote.mjs`), per ADR-002
- [x] Bolt 0 Layer 2 (device-level) verification — happy path confirmed on a real iOS Simulator (host launches, `education` remote loads on demand). Fallback-path proof (remote dev server killed mid-session) and an Android run are still open, non-blocking — see `implement-and-test.md`.
- [x] Bolt 1 Layer 1 — 59 tests passing (7 new suites: auth-guard decision table, auth-session-store, auth-claims, backend-api-client, sign-in/sign-up/verify-email/unconfirmed-email-panel screens).
- [x] Bolt 1 Layer 2 (device-level) — sign-up → verify-email → sign-in flow confirmed working on iOS Simulator. Fix applied: `.env` had malformed `SUPABASE_URL` (extra smart-quote prefix + `/rest/v1/` suffix); corrected to bare project URL. `pod install` required after `.env` changes (react-native-config bakes vars at native build time).
- [x] Bolt 2 Layer 1 — 129 tests passing across 18 suites (70 new tests across 9 new suites: parse-deep-link, validate-totp-code, forgot-password-screen, set-new-password-screen, mfa-challenge-screen, totp-enrollment-screen, account-settings-screen, change-password-screen, change-email-screen).
- [ ] Bolt 2 Layer 2 — deferred to manual verification by user. Rebuild required for `betmeet://` URL scheme (Info.plist updated). Test paths documented in `bolt-2-auth-secondary-flows/implement-and-test.md`.
- [x] Bolt 3 nickname-cooldown reconciliation (ADR-011) — Model-stage error corrected (one free post-onboarding change, not two) against `betmeet-clone`'s real `setNickname` server action before Implement began. `PROFILE-1-nickname.md`'s AC corrected to unambiguous event-based phrasing.
- [x] Bolt 3 Layer 1 — 202 tests passing across 28 suites (73 new tests across 12 new/updated suites: nickname-change-eligibility, validate-nickname-base, validate-avatar-upload, locale, onboarding-wizard-state, locale-store, use-onboarding-wizard, onboarding-wizard-screen, nickname-form, locale-switch, account-settings-screen (updated), auth-guard (updated for the real wizard's route names)). `yarn tsc --noEmit` and `yarn lint` clean (one pre-existing, unrelated lint error in Bolt 2's `supabase-adapter.ts` not touched by this bolt).
- [ ] Bolt 3 Layer 2 — deferred to manual verification by user. `pod install` not yet run in this environment (Ruby/bundler toolchain blocker, see activeContext.md). Test paths documented in `bolt-3-profile-onboarding/implement-and-test.md`.
- [x] Bolt 4 Layer 1 — 229 tests passing across 30 suites (27 new tests across 2 new suites: scoring-rules, compute-score). No new npm deps, no rspack/jest config changes. `yarn tsc --noEmit` clean. `yarn lint` — same one pre-existing error in Bolt 2's `supabase-adapter.ts`, not touched by this bolt. Algorithm cross-checked directly against betmeet-clone's verified source — no reconciliation needed (unlike Bolt 3's ADR-011 case).
- [ ] Bolt 4 Layer 2 — deferred to manual verification by user. No device-surface behavior to verify directly (pure-logic package); Layer 2 integration paths require Bolt 6 (Predictions), Bolt 9 (Rankings), or Bolt 11 (Education) to be implemented first. Test paths documented in `bolt-4-scoring-package/implement-and-test.md`.
- [x] Bolt 5 Layer 1 — 295 tests passing across 44 suites (66 new tests across 14 new suites: match-status, fifa-team-display, fixture-day-grouping, live-update-policy (domain); team-badge, match-card, fixture-list, live-indicator, fixture-day-section, flag-badge, flag-catalog (shared/components); use-fixture-query, use-live-competition-subscription, competition-store (shared/hooks/store)). `yarn tsc --noEmit` and `yarn lint` clean (same one pre-existing, unrelated lint error in Bolt 2's `supabase-adapter.ts`, not touched by this bolt). `@shopify/flash-list` added as a new dependency (first FlashList consumer).
- [x] Bolt 5 Layer 2 — was explicitly deferred to Bolt 6 (this bolt shipped no navigator/screen registration by design). **Now addressed**: Bolt 6 mounts `useFixtureQuery`/`useLiveCompetitionSubscription` on a real screen via its own `PredictionsFixtureList`/`PredictionMatchCard` (ADR-025, reuses Bolt 5's domain logic and hooks but not `FixtureList`/`MatchCard` directly). Bolt 5's six manual test paths are re-scoped and included in `bolt-6-predictions-core/implement-and-test.md`, not yet actually run on a device.
- [x] Bolt 6 Layer 1 — 377 tests passing across 54 suites (82 new tests across 10 new suites: prediction-eligibility, prediction-entry-validation, prediction-score-display (domain); use-predictions-query (hooks); prediction-score-input, penalty-winner-selector, score-breakdown-panel, prediction-match-card, predictions-fixture-list (components); predictions-screen (screen)). `yarn tsc --noEmit` and `yarn lint` clean (same one pre-existing, unrelated lint error in Bolt 2's `supabase-adapter.ts`, not touched by this bolt). No new npm dependency added — bundle-size discipline verified via empty `package.json`/`yarn.lock` diff. First bolt to register a real `Predictions` route on the host `AppStack`.
- [ ] Bolt 6 Layer 2 — not run in this environment (no simulator available). 10 manual test paths documented in `bolt-6-predictions-core/implement-and-test.md`, incl. Bolt 5's six re-scoped paths and two new bolt-6-specific risk checks (kickoff-lock UI correctness, FlashList perf with interactive per-row inputs — both explicitly named risks in bolt-plan.md).
- [x] Bolt 7 Layer 1 — 509 tests passing across 70 suites (132 new tests across 16 new suites: pool-capacity, pool-name, invite-token, invite-permission, pool-membership-permissions, pool-visibility (domain, 67 tests); use-pools-query (hooks, 18 tests); pool-list-item, pool-member-row, invite-token-panel (components, 18 tests); my-pools-screen, discover-pools-screen, create-pool-screen, join-by-token-screen, pool-detail-screen, pool-settings-screen (screens, 29 tests)). `yarn tsc --noEmit` and `yarn lint` clean (same one pre-existing, unrelated lint error in Bolt 2's `supabase-adapter.ts`). `cd backend && npx tsc --noEmit` clean. First bolt to ship the repo's second real Module Federation remote (`src/remotes/pools/`, ADR-032) and the first feature remote since Bolt 0's `education` demo shell.
- [x] Bolt 7 Layer 2 (backend) — **REAL verification, not contract-only** (ADR-030). Booted `backend/` server, minted 3 throwaway Supabase test users via Admin API, exercised all 13 `pools.*` capabilities with curl against the live DB, verified DB state with raw Prisma queries, cleaned up all test data (confirmed zero residue). Proved the capacity-race transactional guard under real concurrency (2 simultaneous joins on a capacity-2/1-member pool → exactly one succeeded, DB confirmed 2/2, no overshoot) and proved ADR-033's "no tournament freeze" invariant against real live competition data (a real LIVE match + a real FINISHED match were present in the seeded DB at verification time; leave/kick/joinPublic/delete all succeeded regardless). Full request/response table in `bolt-7-pools-core/implement-and-test.md`.
- [ ] Bolt 7 Layer 2 (device) — not run in this environment (no simulator available). 12 manual test paths documented in `bolt-7-pools-core/implement-and-test.md`, genuinely runnable now (backend is live, not mocked) — incl. an MF shared-singleton sanity check (ADR-034's one Jest-can't-catch risk: `@tanstack/react-query` must resolve to one `QueryClient` across host+remote).
- [x] Bolt 8 Layer 1 (mobile) — 578 tests passing across 80 suites (69 new tests: 61 across 10 new suites — `directed-invite-target`/`ownership-transfer`/`predictions-visibility`/`pool-override`/`account-deletion` (domain, 33 tests); `pool-override-picker`/`delete-account-screen` (host, 9 tests); `directed-invite-form`/`transfer-ownership-panel`/`prediction-grid-cell` (pools remote, 19 tests) — plus 8 pool-override tests added to `prediction-match-card.test.tsx` and 1 to `account-settings-screen.test.tsx`). `yarn tsc --noEmit` and `yarn lint` clean (same one pre-existing, unrelated lint error in Bolt 2's `supabase-adapter.ts`). `cd backend && npx tsc --noEmit` clean. Two placement ADRs re-examined per the task brief's explicit instruction and reconfirmed unchanged: ADR-032→ADR-036 (pools stays a remote), ADR-024→ADR-037 (predictions' component tier stays host-only). New: ADR-038 (masking server-only + permanent re-verification instruction), ADR-039 (transactional atomicity), ADR-040 (ownership-transfer's two-capability shape).
- [x] Bolt 8 Layer 1 (backend real-DB verification) — **REAL verification, not contract-only** (ADR-030), completed after `backend/.env` was restored mid-session. Booted `backend/` against the live Supabase project, minted 4 throwaway test users via the Admin API, exercised every new/extended capability with curl against the live DB. **Anti-bias masking (ADR-038) verified correct on all three branches at the raw API-response level**: another member's row pre-kickoff is masked exactly per Model §5's field list (`predictedHome`/`predictedAway`/`totalPoints`/`matchedCase: null`, `isOverride`/`hasGlobal: false`, `hidden: true`); the viewer's own row is never masked; another member's row post-kickoff is fully visible (constructed via a direct Prisma insert, since `predictions.save` correctly refuses new writes to an already-locked match). **Dual-save (ADR-039) proved atomic**: happy path wrote both the global and override rows together; failure path (`NOT_MEMBER`) left zero rows written for that match. **Account deletion (ADR-039) proved correct on both paths**: missing-assignment rejection left the pool's `ownerId` and the profile's `deletedAt` both unchanged (verified via direct DB read); the happy path produced all four expected effects (sole-member pool auto-deleted, multi-member pool reassigned with old owner's membership removed, profile soft-deleted with nickname released, auth user hard-deleted). Ownership transfer (POOLS-7/ADR-040) and directed invites (POOLS-3, incl. self-invite/idempotent-reinvite/email-hash-only/permission-denied) all matched Model expectations exactly. A previously-flagged uncertainty resolved: the `auth.users` raw cross-schema email-lookup query works correctly against this project's real DB role. Full request/response detail and the executed runbook: `bolt-8-pools-advanced-account-deletion/implement-and-test.md §3`. Cleaned up all 4 test users/pools/predictions/invites, confirmed zero residue.
- [x] Bolt 9 Model/Design/ADR stages — Model confirmed the current nav shape directly against `root-navigator.tsx`/`screen-registry.ts` (no assumptions); Design resolved the nested Drawer→Tabs→per-tab-stack shell, the MF shared-singleton table `tech-stack.md` had left pending, and the i18n architecture (reusing the `profile.locale` AsyncStorage key's presence as the "explicit choice" signal, no new sentinel); four new ADRs recorded (ADR-042/043/044/045). ADR-041's open question (unify vs. independent locale) was then confirmed by the user (unification).
- [x] Bolt 9 Implement + Layer 1 Test — 594 tests passing across 85 suites (16 new: 6 domain i18n, 3 platform i18n wiring, 1 header-menu-button, 3 main-tab-navigator, 2 root-drawer-navigator, 1 locale-switch unification). `yarn tsc --noEmit` clean; `yarn lint` clean (0 errors, 0 warnings — every warning surfaced during Implement was fixed, not suppressed). ADR-045's Tamagui/Rspack probe found and fixed two real incompatibilities (`tamagui` version bump to `2.4.2`; a `react-dom` `IgnorePlugin` for both the host and `pools`-remote bundles, both of which were actually built to confirm). A previously-unplanned native dependency (`react-native-reanimated`/`react-native-worklets`, `@react-navigation/drawer`'s peer dependency) was discovered mid-Implement and probed for real (`pod install` + `xcodebuild ... -sdk iphonesimulator build` → `** BUILD SUCCEEDED **`), recorded as new ADR-046. Two real bugs caught and fixed before landing: an unstable-navigator-factory anti-pattern in `main-tab-navigator.tsx`'s first draft, and a Jest `import * as X` namespace-mocking gap in the i18n device-detection test. Delivered: the full nav shell, the structural back-button fix (audited every remaining `headerShown:false` occurrence), i18n domain+platform wiring with the confirmed unification behavior, and a bounded, explicitly-documented Tamagui retrofit (nav shell + 4 screens fully retrofitted incl. one in the `pools` remote proving the new MF shared singletons for real; every other Bolt 5-8 screen explicitly listed as a tracked follow-up, not silently dropped). Layer 2 (device) deferred to the user's own manual pass per standing preference. Full detail: `memory-bank/bolts/bolt-9-navigation-i18n-design-retrofit/implement-and-test.md`.
- [x] Bolt 9 second Layer 2 pass (2026-07-06) — the user's own real manual device pass found 5 defects (tab-bar icon missing, clipped button text, hardcoded English in 2 in-scope `pools`-remote files, a cramped My Pools layout, a missing Settings hamburger), all fixed and re-verified at Layer 1 (599 tests, 87 suites, `tsc`/`lint` clean). New ADR-047 records a two-round probe for the icon library (`@tamagui/lucide-icons` rejected after a real install found a duplicate `@tamagui/core@1.x` engine; `lucide-react-native` chosen instead, then a Hermes-parser build bug it surfaced — visible only via a real dev-server build, not a static one — fixed via `ReanimatedPlugin({unstable_disableTransform:true})` + an explicit loader exclude). Full detail: `implement-and-test.md §11`. Awaiting the user's retest of all 5 fixes before Layer 2 is considered closed.
- [~] Bolt 8 Layer 2 (device) — **in progress** (2026-07-03, user's own manual pass, per standing preference). Two real bugs found and fixed so far, both the class Layer 1's Jest suite structurally cannot catch: (1) onboarding completion left the user stuck on the wizard — `getSession()` doesn't trigger `onSessionChange`; fixed with a new `SupabaseAdapter.refreshSession()`, now used by `onboarding-wizard-screen.tsx`. (2) The `pools` remote crashed on first navigation (`Invariant Violation: Tried to register two views with the same name RNSVGCircle`) — `react-native-svg` wasn't an MF shared singleton, so host and remote each registered their own copy of the same Fabric native component; fixed by adding it to both `rspack.config.mjs` and `rspack.config.pools-remote.mjs`'s shared config (same category of gap ADR-034 already flagged for `@shopify/flash-list`/`@tanstack/react-query`). Full writeup: `bolt-8-pools-advanced-account-deletion/implement-and-test.md §4.1`. Remaining manual test paths in `§4` (incl. the pre-existing MF shared-singleton sanity check for cross-bundle query invalidation) are unblocked and exercisable now.

## Bolts (Execution Units)

| Bolt | Scope | Depends on | Risk | Status |
|---|---|---|---|---|
| 0 | Platform scaffolding (Module Federation, Supabase adapter, Backend-API client skeleton) | — | Medium | **Done** (Layer 1 ✅, Layer 2 happy-path ✅) |
| 1 | Auth core (sign-in/up, navigation guard, secure session storage) | Bolt 0 | High | **Done** (Layer 1 ✅, Layer 2 happy-path ✅) |
| 2 | Auth secondary flows (OAuth, MFA, password reset, change password/email) | Bolt 1 | Medium-High | **Layer 1 ✅ (129 tests)** / Layer 2 pending manual |
| 3 | Profile & onboarding wizard | Bolt 1 | Medium | **Layer 1 ✅ (202 tests)** / Layer 2 pending manual |
| 4 | Scoring package *(parallelizable with 1–3)* | Bolt 0 | Low | **Layer 1 ✅ (229 tests)** / Layer 2 pending manual |
| 5 | Competition read model (fixtures, live updates, team/flag data) | Bolt 1 | Low-Medium | **Layer 1 ✅ (295 tests)** / Layer 2 addressed via Bolt 6 (not yet run on device) |
| 6 | Predictions core | Bolt 3, 4, 5 | Medium | **Layer 1 ✅ (377 tests)** / Layer 2 pending manual |
| 7 | Pools core | Bolt 3 | Low-Medium | **Layer 1 ✅ (509 tests) / Layer 2 backend ✅ real / Layer 2 device pending manual** |
| 8 | Pools advanced + predictions↔pools integration + account deletion | Bolt 6, 7 | Medium-High | **Layer 1 ✅ mobile (578 tests) / Layer 1 backend ✅ real / Layer 2 device in progress — 2 bugs found+fixed** |
| 9 | **Navigation, i18n & Design Retrofit** (bottom tabs + drawer, iOS back-button fix, i18next/react-native-localize, Tamagui) — inserted 2026-07-03 per approved change request | Bolt 8 | Medium-High | **Layer 1 ✅ (599 tests)** — 5 real Layer 2 findings fixed (icons, button clipping, pools-remote i18n misses, My Pools layout, Settings hamburger), awaiting user's retest. See `bolt-9-navigation-i18n-design-retrofit/model.md`/`design.md`/`implement-and-test.md`/ADR-042 through ADR-047. |
| 10 | Scoring & rankings *(renumbered from 9, 2026-07-03)* | Bolt 6, 8, 9 | Medium | Not started |
| 11 | Notifications (native push) *(renumbered from 10)* | Bolt 1, 3, 9 + FCM/APNs infra setup | High | Not started |
| 12 | Education (Rules Center) *(renumbered from 11)* | Bolt 3, 4, 9 | Low | Not started |
| 13 | Admin *(renumbered from 12)* | Bolt 5, 10 | Low-Medium | Not started |

Full detail, sequencing diagram, and risk rollup: `memory-bank/intents/liga-mundial-mobile-migration/bolt-plan.md`.

## Deferred or Blocked Tasks
- ~~Resolve dual lockfiles~~ — **done in Bolt 0** (Yarn only).
- Remove or repurpose `metro.config.js` once Re.Pack is confirmed as the sole bundler (now provably dead, not yet deleted).
- Set real iOS bundle identifier and app display name before release.
- Bolt 0's Layer 2: fallback-path proof (kill the remote's dev server mid-session, confirm `RemoteBoundary`'s retry UI instead of a crash) and an Android Simulator/Emulator run — not yet done, non-blocking (see `memory-bank/bolts/bolt-0-platform-scaffolding/implement-and-test.md`).
- ~~Real env-var injection for `SUPABASE_URL`/`SUPABASE_ANON_KEY`~~ — **done in Bolt 1** (`.env` with real project values; `pod install` required after any `.env` change). `BACKEND_API_BASE_URL` still empty — TBD when backend hosting is decided.
- Push-SDK choice (Expo Notifications vs. direct FCM/APNs SDKs) and FCM/APNs project/certificate setup — must be resolved before Bolt 10 opens, not mid-bolt.
- Where the backend API contract mobile depends on physically lives (inside `betmeet-clone` vs. a separate service) — out of this repo's authority; needed before any bolt that calls it can integrate end-to-end.
- Repo's HEAD is detached at the initial commit (not on a branch) — pre-existing, unrelated to Bolt 0, flagged for whenever commits are made.
- **Bolt 5: real flag artwork not yet vendored** — `src/shared/competition/flags/` currently renders placeholder colored swatches, not the real `lipis/flag-icons` SVG set (no network access in this environment). Must be resolved before any release build; a content-only change (`flag-catalog.ts`), no consumer code changes needed. See activeContext.md Known Issues. Inherited unchanged into Bolt 6 (Predictions reuses `TeamBadge`/`FlagBadge` as-is).
- **Bolt 5: Realtime channel/event naming unconfirmed** — `subscribeToLiveResults()`'s `live-results`/`results-updated` naming is a best-guess against the domain description, not yet verified against a real backend contract; isolated behind the adapter seam. Inherited unchanged into Bolt 6.
- **Bolt 6: `predictions.*`/`competition.getKnockoutPhaseIds` capability contracts unconfirmed against a real backend** — same category of gap as other `*-api.ts` seams; contract-only, Layer 1 mocks them.
- **Bolt 6: `getFixtureWithMyPredictions` is not a real backend endpoint** — this bolt joins fixture + predictions client-side (`useMatchesWithMyPredictions`) instead; isolated to one function if a combined endpoint is added later.
- **Bolt 6: PREDICTIONS-3/PREDICTIONS-4 (pool override/dual-save, reset override) explicitly out of scope**, deferred to Bolt 8 per bolt-plan sequencing — types are shaped to accept the future fields (`poolId`) but no UI sends them yet.
- **Bolt 6: the `units/unit-05-predictions/` story files referenced by bolt-plan.md are not present in this repo** — scope was reconciled against `domain-overview.md`/`system-context.md` directly; no ambiguity requiring a reconciliation ADR was found (unlike Bolt 3's ADR-011).
- **Bolt 7: `@react-native-clipboard/clipboard` not installed** — `invite-token-panel.tsx`'s copy button uses an injectable `onCopy` prop (no-op default + visible `selectable` text fallback) rather than adding a new native dependency mid-bolt. Isolated follow-up to one component when the package is added.
- **Bolt 7: POOLS-3/POOLS-6/POOLS-7 (directed invites, predictions-grid anti-bias masking, ownership transfer) explicitly out of scope**, deferred to Bolt 8 per bolt-plan sequencing — `pool-settings-screen.tsx`'s delete flow never references a transfer option since none exists yet.
- **Bolt 7: the `units/unit-06-pools/` story files referenced by bolt-plan.md are not present in this repo** — scope was reconciled directly against `domain-overview.md §5.3`; no ambiguity requiring a reconciliation ADR was found (same precedent as Bolt 5/6).
- ~~Bolt 7: the `pools` remote's own dev server (port 8083) has not been started/proven in this environment~~ — **now proven, and one real bug found + fixed**: `yarn start:pools` script added to `package.json`; the remote loaded but crashed on its first real navigation until the `react-native-svg` MF-shared-singleton gap was fixed (see Bolt 8 Layer 2 entry above). Manual test paths #11/#12 in `bolt-7-pools-core/implement-and-test.md` are unblocked now.
- **Every MF shared-dependency list needs a deliberate audit, not just incremental fixes as gaps are hit on-device** — `react-native-svg` was the second Jest-can't-catch MF singleton gap found (after ADR-034's `@tanstack/react-query` risk, which was flagged proactively; this one wasn't). Worth doing before Bolt 9 adds any more shared UI reuse across bundles.
- ~~Bolt 8: real backend Layer 1 verification blocked by a missing `backend/.env`~~ — **resolved**: credentials were restored mid-session and the full curl runbook was run for real against the live Supabase DB (masking, dual-save happy+failure path, directed invite, ownership transfer, account deletion happy+missing-assignment-failure path, cleanup) — zero discrepancies found. See `bolt-8-pools-advanced-account-deletion/implement-and-test.md §3`.
- **Bolt 8: POOLS-7 (ownership transfer) intentionally ships as a mobile-specific product addition beyond `betmeet-clone`** — the web app only ever transfers ownership as an account-deletion side-effect; this bolt adds a standalone "Transfer ownership" affordance in Pool Settings too (Model-stage-approved, ADR-040, now verified for real). Not a porting gap if a future audit compares against `betmeet-clone` and finds this extra affordance.
- **Bolt 8: PoolDirectedInvite accept/revoke status transitions (`ACCEPTED`/`REVOKED`/`EXPIRED`) are not built** — confirmed absent in `betmeet-clone`'s own real code too; a directed invite only ever shares the pool's existing invite token with one named person.
- **Bolt 8: `POOL_INVITE` notification queuing and nickname-search/typeahead for the directed-invite target field are both explicitly deferred** — Bolt 10 owns notification delivery infrastructure (doesn't exist yet); typeahead is a UX-polish layer, isolated follow-up.
