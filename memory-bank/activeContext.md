# Active Context

> **Agent note:** This is your short-term memory. Read it at the start of every session and update it immediately after making an important decision, changing focus, or encountering a blocker.

## Current Focus
- **Bolt 7 (Pools core) is closed at Layer 1, and Layer 2 backend verification is REAL and DONE (2026-07-02)** — not contract-only, unlike every prior bolt's deferred Layer 2. 509 tests green (up from 377, +132: 67 domain + 65 remote across 16 new suites). Delivered `src/domain/pools/` (capacity/name/invite-token/invite-permission/membership-permission/visibility rules, all framework-free, all advisory-only) + `src/remotes/pools/` — **the repo's second real Module Federation remote and first real feature remote** (ADR-032, `education` remains the only prior remote and was always a demo shell) — six screens (my-pools/discover/create/join-by-token/detail/settings), FlashList-based lists, TanStack Query hooks. New `pools-api.ts` capability seam (13 capabilities). Host wiring: new `rspack.config.pools-remote.mjs`, `pools` remote entry + `@shopify/flash-list`/`@tanstack/react-query` added to both hosts' and the new remote's MF `shared` config (ADR-034 — first non-host consumers of both), `Pools` route on `AppStack` (`['protected']`, mounted via `lazy`+`RemoteBoundary` same as `education`). **Backend side (ADR-030): 13 new `pools.*` handlers in `backend/src/routes/handlers.ts`**, real invite-token generation (`backend/src/services/pool-invite-token.ts`), real permission checks (`backend/src/services/pool-permissions.ts`). No new Prisma migration — `Pool`/`PoolMembership` already existed (backend-phase1 provisioning). **Verified for real**: booted `backend/` server, minted 3 throwaway Supabase test users via Admin API, exercised every capability with curl against the live DB (create/rename/delete/visibility/membersCanInvite/joinByToken/joinPublic/leave/kick/setArchived/getMine/listPublic/getDetail), **proved the capacity-race transactional guard under real concurrency** (two simultaneous joins on a capacity-2/1-member pool — exactly one succeeded, DB-verified 2/2 members, no overshoot), **proved ADR-033's "no tournament freeze" rule against real live competition data** (the seeded DB already has a real LIVE match and a real FINISHED match — leave/kick/joinPublic/delete all succeeded against that live state), then fully cleaned up (pools deleted, profiles deleted, all 3 auth users deleted, confirmed zero residue). See `memory-bank/bolts/bolt-7-pools-core/implement-and-test.md` for the full request/response table. 12 device-level manual test paths documented, genuinely runnable now (backend is live) — not yet run in this environment (no simulator).
- **Backend Phase 1 is LIVE (2026-07-01), not just code-complete.** Architecture pivot ADR-026 through ADR-029 (`memory-bank/bolts/bolt-backend-phase1/`) — backend lives **inside this repo** as a standalone `backend/` Node/Express service, connected to the user's own Supabase project. `betmeet-clone` untouched/independent. Verified end-to-end against the real database with a throwaway test user (created and cleaned up via Admin API): `profile.getProfile`, `competition.getFixture` (57 real matches), `predictions.save`/`getMyPredictions`, and all three business outcomes (`LOCKED` on a past-kickoff match, `VALIDATION_FAILED` on a tied-knockout prediction missing a penalty winner, successful save with one). Server runs via `cd backend && npm run dev`, `http://localhost:4000`, mobile's `.env` already points `BACKEND_API_BASE_URL` at it.
- **Important correction to the original Phase 1 plan**: the user's Supabase project was **not empty** — it already had `betmeet-clone`'s full schema, RLS, the Access Token Hook function, and real seed data (48 teams, 52 World Cup 2026 matches, `_prisma_migrations` table proving betmeet-clone's actual migrations had been run against it) applied at some earlier point. Rather than overwrite this with the originally-planned leaner Phase 1 schema (`prisma db push` would have **dropped** `provider_sync_runs`/`push_subscriptions`/`notification_events`/`notification_deliveries`/`prediction_scores` and several real columns — caught before executing, confirmed with the user, did not proceed), the backend's `schema.prisma` was regenerated via `prisma db pull` to match the live database exactly. `backend/prisma/sql/001-access-token-hook.sql`/`002-prediction-lock-guard.sql` turned out to be **unneeded** — both the hook function and the `prediction_lock_guard` trigger already existed; only the Dashboard-side "enable the hook" toggle was still off, now confirmed enabled (fresh test JWT carries `email_verified`/`onboarding_completed`/`account_deleted`). `backend/src/scripts/seed-competition.ts` was changed from a from-scratch seed to a **purely additive** supplement (5 matches, `matchNumber` 9001-9005, reusing existing phases) since the real data had no FINISHED match and no upcoming match with both teams resolved — ran once, safe to re-run (idempotent upsert).
- Mobile-side JWT claim dependency (`supabase-adapter.ts:165-167`, Bolt 1's `AuthGatedNavigator`) is now satisfied for real — no outstanding action here.
- **Bolt 6 (Predictions core) is closed at Layer 1** — 377 tests green (up from 295). This is the first bolt to mount Bolt 5's fixture data on a real, navigable screen — a new `Predictions` route is registered on the host's `AppStack` (`['protected']` tag), reachable from a new button on `HomeScreen`. Layer 2 not run in this environment (no simulator available) — 10 manual test paths documented in `memory-bank/bolts/bolt-6-predictions-core/implement-and-test.md`, including **Bolt 5's six previously-deferred paths, now re-scoped and exercisable** through this bolt's own `PredictionsFixtureList`/`PredictionMatchCard` components (which reuse Bolt 5's `buildFixtureView`/`useFixtureQuery`/`useLiveCompetitionSubscription` but not its `FixtureList`/`MatchCard` — see ADR-025).
- Bolt 6 delivered: `src/domain/predictions/` (kickoff-lock state machine ported verbatim from `domain-overview.md §4.2`, entry validation incl. penalty-winner-iff-tied-knockout rule, score-breakdown display adapter reusing Bolt 4's `computeScore()` — framework-free) + `src/host/predictions/` (host-only RN layer: `PredictionScoreInput`/`PenaltyWinnerSelector`/`ScoreBreakdownPanel`/`PredictionMatchCard`/`PredictionsFixtureList` components, `use-predictions-query.ts` hooks incl. a client-side fixture+prediction join, `PredictionsScreen`). New `predictions-api.ts` capability seam (`predictions.getMyPredictions`/`predictions.save`); `competition-api.ts` additively gained `getKnockoutPhaseIds()` (Bolt 5's `getFixture()` untouched). 82 new tests across 10 new suites. No new npm dependency, no rspack/jest config change.
- **Important scope note:** the `memory-bank/intents/liga-mundial-mobile-migration/units/` story files the bolt-plan references (e.g. `PREDICTIONS-1-*.md`) are **not present in this repo** (same gap noted, but not previously called out explicitly, in Bolt 5's `model.md`) — scope for PREDICTIONS-1/2/5 was reconciled directly against `domain-overview.md §4.2`/`§5.4`/`§5.5` and `system-context.md §3` instead. The prose there was unambiguous, so no reconciliation ADR (à la Bolt 3's ADR-011) was needed.
- **Previously (Bolt 5):** Competition read model closed at Layer 1 — 295 tests green (up from 229), Layer 2 deferred to Bolt 6 (now addressed above). Delivered `src/domain/competition/` (day-grouping, linger-window decision, ±3h live-update-relevance heuristic, FIFA/flag display rules) + `src/shared/competition/` (FlashList-based fixture list/match-card/team-badge components, `use-fixture-query`/`use-live-competition-subscription` hooks, a Zustand store scoped to the past-matches toggle). `SupabaseAdapter` extended with `subscribeToLiveResults()`. `@shopify/flash-list` added (first FlashList consumer).
- **Bolt 4 (Scoring package) is closed at Layer 1** — 229 tests green (up from 202). Layer 2 deferred to manual verification by user (no device-surface behavior in this pure-logic package; Layer 2 test paths require consuming screens from Bolt 6/9/11 — see `memory-bank/bolts/bolt-4-scoring-package/implement-and-test.md`).
- Bolt 4 delivered: `src/shared/scoring/` — `ScoringRuleSet` constants, `computeScore()` pure function, `derivePenaltyWinner()` helper, barrel `index.ts`. 27 new tests across 2 new suites. No new npm deps, no rspack/jest config changes.
- **Bolt 3 (Profile & onboarding) is closed at Layer 1** — 202 tests green (up from 129). Layer 2 deferred to manual verification by user; `pod install` not yet run in this environment (Ruby/bundler toolchain blocker — see Known Issues).
- Bolt 3 delivered: nickname assignment/cooldown (PROFILE-1), avatar 3-source picker + upload (PROFILE-2), locale preference (PROFILE-3), the real 5-step onboarding wizard replacing the Bolt-1 placeholder (PROFILE-4), Settings "Profile" section (PROFILE-5).
- **Important correction (Bolt 3, ADR-011):** the nickname cooldown rule is **one** free post-onboarding nickname change, then a 30-day cooldown — not two. This was a Model-stage error caused by ambiguous AC wording in `PROFILE-1-nickname.md` (now corrected). Resolved by direct investigation of `betmeet-clone`'s real `setNickname` server action — see `memory-bank/bolts/bolt-3-profile-onboarding/adr-011-nickname-cooldown-reconciliation.md` for the full evidence trail. The regression test for this is `src/domain/profile/__tests__/nickname-change-eligibility.test.ts`.
- Bolt 2 (Auth secondary flows) is closed at Layer 1 — 129 tests green. Layer 2 deferred to manual verification by user.
- Bolt 2 delivered: Google OAuth + deep link (AUTH-2), TOTP MFA enrollment + challenge screen (AUTH-3), forgot/reset password via deep link (AUTH-4), change password + change email in Settings (AUTH-5).
- App must be rebuilt (not just restarted) for `betmeet://` URL scheme to be active on iOS — `Info.plist` was updated.

## Recent Technical Decisions
- **Inception-level decisions** (binding, `requirements.md §7`): backend architecture = Option A; single Supabase-access adapter mandatory; no code dependency on `betmeet-clone`; Module Federation placement per feature (auth/profile/predictions/notifications-permission-logic → host; pools/competition/scoring-rankings/notifications-preferences/education/admin → remote; `scoring` → shared library); TOTP-only MFA at launch; FCM+APNs direct; `react-native-keychain`/`react-native-image-picker`/`react-native-svg`/`betmeet://`+Universal/App Links required; iOS+Android only, no offline support.
- **Bolt 0 ADRs (implemented)**: layered folders `domain/platform/shared/host/remotes`; host + example `education` remote; React Navigation (native-stack); TanStack Query (server) + Zustand (client); `scoring` as shared singleton under `src/shared/scoring/`; `@/*` → `src/*` alias.
- **Bolt 1 ADRs (implemented)**:
  - ADR-001: navigation guard via conditional screen-tree rendering (not action interception) — `AuthGatedNavigator` renders one of five branches based on `evaluateGuard` outcome.
  - ADR-002: Zustand auth-session store — holds only derived `claims` (never raw tokens), `status: loading|ready`, `pendingDestination`.
  - ADR-003: sign-in/sign-up/resend classification logic lives in `domain/auth/` (pure functions, no SDK import); `SupabaseAdapter` imports and calls them, never the reverse.
  - ADR-004: `ScreenClass` is a tag array (not single-pick enum); `PendingConfirmationScreen` + `VerifyEmailScreen` merged into one `VerifyEmailScreen` branching on `reason` param.
- **Bolt 2 ADRs (implemented)**:
  - ADR-005: deep-link handler via RN core `Linking` API + React Navigation `LinkingConfiguration` (no extra library).
  - ADR-006: OAuth on Simulator uses Safari + `betmeet://` custom-scheme callback; PKCE exchange is server-side in Supabase.
  - ADR-007: `react-native-qrcode-svg` (wraps `react-native-svg`) for TOTP QR rendering — `pod install` required.
  - ADR-008: `AuthGatedNavigator` extended with `mfa-challenge` branch (aal1 session + nextLevel aal2) — ADR-001 extended, not violated.
  - ADR-009: `MfaProvider` interface designed as passkey seam; no implementation in this bolt.
- **Bolt 3 ADRs (implemented)**:
  - ADR-010: onboarding's final step (`second-factor`) is a TOTP-enrollment nudge reusing Bolt 2's `TotpEnrollmentScreen` — replaces the web app's deferred passkey step.
  - ADR-011: nickname-cooldown reconciliation — one free post-onboarding change (not two); full evidence trail from `betmeet-clone`'s real `setNickname` action.
  - ADR-012: locale defaults to `es` regardless of device language (explicit, recorded UX call, matches web app).
  - ADR-013: `@react-native-async-storage/async-storage` for locale local-persistence (new native dep, distinct from Keychain's session-only scope). Jest-mocked via `moduleNameMapper` → the package's own `./jest` subpath export, not `setupFiles`.
  - ADR-014: avatar default-set picker uses a plain grid, not FlashList — bounded/small list, not a list-performance case.
  - `profile.*` capability group added to the `BackendApiClient` seam (`src/platform/backend-api/profile-api.ts`) — no new transport, same `request()` pattern as Bolt 1's `auth.resendConfirmation`.
  - `OnboardingStackParamList`/`screen-registry.ts` replaced the single Bolt-1 placeholder route with 5 real wizard routes, all still tagged `['onboarding']` — `AuthGatedNavigator`'s guard branch logic (ADR-001/ADR-008) untouched.
- **Bolt 4 ADRs (implemented)**:
  - ADR-015: `scoring` is NOT added to the MF `shared` config — it is a stateless pure-function module; single-source guarantee is structural (one physical `src/shared/scoring/` directory, same `@` alias in every rspack config). Supersedes ADR-002/005's anticipation of adding it to `shared`. `rspack.config.mjs` and `rspack.config.education-remote.mjs` unchanged.
  - ADR-016: duplicate-detection gate — `@invariant` JSDoc in source files + this ADR as the architectural record + a three-item code-review checklist applied at Bolt 6/9/11/12 PR review. No new tooling.
- **Bolt 5 ADRs (implemented)**:
  - ADR-017: `competition` ships as a filesystem-shared library (`src/shared/competition/` + `src/domain/competition/`), **not** a federated remote — resolves `requirements.md §7.4`'s "Remote or shared" ambiguity. Driven by Predictions (Bolt 6, host-placed, highest-frequency screen) having a render-blocking dependency on competition's match/team data. Supersedes `system-context.md §4`'s topology diagram for the data/domain layer, same pattern as ADR-015 for `scoring`. A future standalone browse-only fixture screen remains a legitimate future remote candidate.
  - ADR-018: two-tier `domain/` (framework-free) + `shared/` (React/RN components/hooks/store) split for `competition` — diverges from `scoring`'s single-tier shape since `competition` has substantial UI, unlike `scoring`.
  - ADR-019: one flat `['competition', 'fixture']` TanStack Query key; `FixtureView`'s day-grouping/linger-window decision is derived via `select` at read time against the live clock, never cached as the query result (it depends on "now," which a cached object can't represent without going stale).
  - ADR-020: Zustand `competition-store.ts` scoped to only the past-matches toggle; `LiveSubscriptionState` deliberately stays local to its hook (single owner/consumer) rather than promoted to a store.
  - ADR-021: Realtime Broadcast subscription via the `SupabaseAdapter` seam (`subscribeToLiveResults()`), with a **mandatory** (not error-only-triggered) polling fallback, and unconditional full re-evaluation on every foreground transition — never resumes a possibly-stale pre-background subscription handle.
  - ADR-022: `@shopify/flash-list` added as a new dependency — first FlashList consumer in the repo, for the ~104-match grouped fixture list. Sets the precedent for future list-heavy bolts (pools, rankings).
- **Bolt 6 ADRs (implemented)**:
  - ADR-023: client-side kickoff-lock check (`getPredictionEligibility`) is explicitly advisory-only, never the actual authorization gate — the backend API + `prediction_lock_guard` DB trigger are the real, structural enforcement (`requirements.md §7`) regardless of this bolt's code. No optimistic-save UI pattern was introduced; a `LOCKED` save-rejection is handled as an expected outcome, not an exceptional error.
  - ADR-024: Predictions' RN component/hook tier stays **host-only** (`src/host/predictions/`) — no `src/shared/predictions/` tier created, since there is exactly one bundle consumer today (unlike `competition`, which had two from day one, justifying ADR-018's two-tier split). Promote on second consumer (e.g. if Bolt 8 or Bolt 9 need to reuse this UI), not preemptively. The domain tier (`src/domain/predictions/`) is already framework-free and shared-root, unaffected by this decision.
  - ADR-025: new host-owned `PredictionsFixtureList`/`PredictionMatchCard` components, **not** a generic row-renderer prop added to Bolt 5's `FixtureList`/`MatchCard` — avoids eroding a filesystem-shared library's single-purpose shape (same discipline as ADR-015/ADR-017's "one physical implementation" invariant, applied to a component). Reuses `buildFixtureView`/`FixtureDaySectionHeader`/`TeamBadge`/`LiveIndicator`; duplicates only ~40 lines of FlashList wiring. Bolt 5's `FixtureList`/`MatchCard` are untouched.
  - `predictions.*` capability group added (`src/platform/backend-api/predictions-api.ts`) — same `request()` pattern as `competition-api.ts`/`profile-api.ts`. `competition.getKnockoutPhaseIds()` added additively to the existing `competition-api.ts` (closes a real gap: `Match.phaseId` existed but no phase-type lookup was ever exposed, since Bolt 5 never needed to distinguish group/knockout/league phases).
  - `Predictions` route registered directly on the existing `AppStack` (no new stack navigator — single screen, not a multi-step flow), tagged `['protected']` in `screen-registry.ts`, same pattern as `Home`.
- **Bolt 7 ADRs (implemented)**:
  - ADR-032: `pools` ships as this repo's **second real Module Federation remote** (`src/remotes/pools/`), not folded into shared-library status the way `competition` was (ADR-017). Investigated fresh against this bolt's concrete scope (per the task brief's explicit ask, not a rubber-stamp of Inception's placement table): `domain-overview.md §7`'s cross-feature dependency map has **no `predictions → pools` edge** (the exact edge that forced ADR-017 for `competition`), and Bolt 8's predictions↔pools integration reads as a narrow "which pool to override" picker pull, not a render-blocking dependency on pools' full UI. `pools` remains the single new `AppStack` entry (`Pools`), mounting the remote's own internal `NativeStackNavigator` (one exposed `./App` module, zero host-side knowledge of pools' internal routes).
  - ADR-033: **explicit, permanent record that no "tournament freeze" gate exists on any pools membership action** (join/leave/kick/delete) — an earlier web-app rule removal that `domain-overview.md §5.3` warns not to reintroduce by accident (e.g. by pattern-matching against predictions' still-valid kickoff lock, a structurally different gate on a different resource). Verified both structurally (no function signature anywhere in `pool-membership-permissions.ts`/`pool-permissions.ts`/the five mutating backend handlers accepts a competition/match input) and empirically (real curl calls against the live DB with an actual LIVE + FINISHED match present — all four actions succeeded).
  - ADR-034: the `pools` remote owns one internal stack navigator (not five host-registered routes); `@shopify/flash-list` **and** `@tanstack/react-query` added to both `rspack.config.mjs`'s and the new `rspack.config.pools-remote.mjs`'s MF `shared` config as singletons — first non-host consumers of either. The React Query addition is load-bearing for correctness (not just bundle size): without `singleton: true`, the remote's `useQuery`/`useMutation` calls would resolve against a different QueryClient/Context module instance than the host's provider populated. Flagged as the one MF-specific risk Jest (single-bundle test runner) cannot catch — only a real two-bundle device run proves it; documented as manual test path #12 in `implement-and-test.md`.
  - ADR-035: pools' two race-prone rules (capacity, public-name uniqueness) are closed **transactionally**/**at the DB unique-index layer** on the backend, never trusted to the mobile domain's advisory pre-checks — mirrors ADR-023's "advisory client, authoritative server" discipline, applied to a second, structurally different resource. Proven under real concurrency (two simultaneous join requests against a capacity-2/1-member pool), not just asserted.
  - `pools.*` capability group added (`src/platform/backend-api/pools-api.ts`, `backend/src/routes/handlers.ts`) — same `request()`/`{ok:true,...}|{ok:false,error:...}` pattern as every prior `*-api.ts`/handler group.
- **Env-var setup (resolved in Bolt 1)**: `react-native-config` reads `.env` at native build time — `pod install` is required after any `.env` change. `SUPABASE_URL` must be the bare project URL (`https://<ref>.supabase.co`), no path suffix. `BACKEND_API_BASE_URL` — being resolved by the backend-phase1 work (see Current Focus); points at the new `backend/` service once running locally.
- **RESOLVED (backend-phase1 audit of `betmeet-clone`)**: the Realtime channel/event naming was confirmed, not guessed — `betmeet-clone`'s `src/features/competition/live-results-channel.ts` defines `LIVE_RESULTS_CHANNEL = "live-results"`, `RESULTS_UPDATED_EVENT = "results-updated"`, exactly matching Bolt 5's best-guess. No change needed to `subscribeToLiveResults()`.
- Still open, deferred: concrete performance budget numbers; push-SDK choice (Expo Notifications vs. direct FCM/APNs — now planned as backend Phase 4, ahead of Bolt 10).

## Known Issues / Blockers
- `metro.config.js` still exists but is provably dead — safe to delete in a future cleanup bolt.
- iOS bundle identifier is still the RN CLI placeholder — needs updating before any release build.
- `ios/BetmeetMobile.xcworkspace/`, `ios/Podfile.lock`, and `Gemfile.lock` are untracked — pre-existing, unrelated to Bolt 1.
- Repo's HEAD is detached at the initial commit (not on a branch) — pre-existing; flagging since the next `git commit` would not land on any branch until resolved.
- Bolt 0 Layer 2: fallback-path proof (kill `education` remote dev server mid-session, confirm `RemoteBoundary` retry UI) and Android run — still open, non-blocking.
- FCM/APNs project setup needed before Bolt 10. AUTH-6 is sequenced into Bolt 8 (needs `unit-06-pools`'s ownership-transfer first).
- Stale `HERMES_CLI_PATH` in `Pods-BetmeetMobile.debug.xcconfig` still points at the old `dynamicdevs/futbol/...` path — doesn't block Simulator/dev-server runs but would break a Release build; rerun `cd ios && bundle exec pod install` if hit.
- Node 20 is the shell's default; Re.Pack / Jest / Yarn require Node ≥ 22.11.0 — run `nvm use 22` before any `yarn` command in a new terminal.
- **backend/.env Postgres connection gotchas (hit and resolved during Phase 1 setup, will recur for any future Supabase project on this stack):** (1) Supabase's dashboard connection-string UI shows the password as a placeholder in brackets — easy to copy an extra `]` by accident; verify `DATABASE_URL`/`DIRECT_URL` have the *identical* password if a P1000 auth error appears, or just reset the DB password and repaste carefully. (2) Supabase's plain "Direct connection" (`db.<ref>.supabase.co:5432`) is **IPv6-only** by default (no IPv4 add-on) and gets `ECONNREFUSED` on most networks — use the **Session pooler** URI instead for `DIRECT_URL` (same `aws-x-region.pooler.supabase.com` host as the transaction pooler, just port `5432` instead of `6543`, no `pgbouncer=true` needed).
- **Bolt 3: `pod install` not yet run** for `react-native-image-picker`/`@react-native-async-storage/async-storage` — this environment's system Ruby (2.6.10) can't satisfy the `Gemfile.lock`'s bundler 2.7.2 requirement; an `rbenv`-managed Ruby 3.3.5 exists on the machine but its shim wasn't reachable from a non-interactive shell in this session. Must be resolved (e.g. `eval "$(rbenv init -)"` in an interactive shell, then `cd ios && bundle install && bundle exec pod install`) before any device build touching Bolt 3's avatar/locale screens.
- `BACKEND_API_BASE_URL` still empty — being resolved by backend-phase1 (see Current Focus); until the local `backend/` service is running and `.env` points at it, Bolt 3/5/6's capabilities remain contract-only (Layer 1 mocks them).
- **Bolt 5: flag artwork is a placeholder, not real vendored SVGs.** `src/shared/competition/flags/flag-catalog.ts`/`flag-badge.tsx` implement the full bundled, keyed-lookup architecture COMPETITION-3 requires (including the three UK home-nations subdivision keys `gb-eng`/`gb-sct`/`gb-wls` kept structurally distinct from any ISO code), but render simple colored-rectangle swatches rather than the real `lipis/flag-icons` artwork referenced in `migration-analysis.md`/`project-inventory.md` (`public/flags/`) — no network access was available in this environment to fetch and vendor the real SVG set. Swapping in real artwork is a content-only change to `flag-catalog.ts` (consumers address flags only by `FlagAssetKey`, so no consumer code changes). **Must be resolved before any release build** — not a blocker for Bolt 6.
- ~~Bolt 5: Realtime channel/event naming is a best guess~~ — **confirmed correct** by the backend-phase1 audit of `betmeet-clone` (see Current Focus/Env-var setup above). No change needed.
- **Bolt 6: `predictions.getMyPredictions`/`predictions.save`/`competition.getKnockoutPhaseIds` capability contracts are unconfirmed against a real backend.** Being resolved by backend-phase1 Phase 1, which implements handlers for exactly these capability names.
- **Bolt 6: `getFixtureWithMyPredictions` is not a real backend endpoint.** This bolt joins Bolt 5's fixture query with its own `predictions.mine` query client-side (`useMatchesWithMyPredictions`) rather than assuming a combined endpoint exists. Swapping to a real combined endpoint later, if one is added, is isolated to that one join function.
- **Bolt 6: PREDICTIONS-3 (pool override + dual-save) and PREDICTIONS-4 (reset override) are explicitly out of scope**, deferred to Bolt 8 per bolt-plan sequencing. `MyPrediction.poolId`/`SavePredictionInput.poolId` are already shaped to accept a non-null value so Bolt 8 doesn't need to widen these types, but no UI in Bolt 6 ever sends one — every prediction saved so far is global.
- **Bolt 7: `@react-native-clipboard/clipboard` is not installed** — `Clipboard` was removed from react-native core in this RN version. `invite-token-panel.tsx`'s copy button is wired via an injectable `onCopy` prop (no-op default, "Copied!" visual feedback + `selectable` text as a manual fallback) rather than adding a new native dependency mid-bolt without being asked. Adding the real package is an isolated follow-up to that one component.
- **Bolt 7: POOLS-3/POOLS-6/POOLS-7 (directed invites, predictions-grid anti-bias masking, ownership transfer) are explicitly out of scope**, deferred to Bolt 8 per bolt-plan sequencing — `pool-settings-screen.tsx`'s delete flow deliberately never references a transfer option, since none exists yet.
- **Bolt 7: `pools` remote's dev server (port 8083) has not actually been started/proven in this environment** — the wiring (rspack config, ScriptManager resolver entry) mirrors the already-proven `education`/port-8082 pattern exactly, but an actual `npx re-pack start` run for the `pools` remote, and the resulting host→remote chunk download, has not been exercised on a device here (same "no simulator available" constraint as every prior bolt's Layer 2). Manual test path #11/#12 in `implement-and-test.md` cover this once a Simulator is available.

## Immediate Next Step
- **Bolt 7 is closed at Layer 1, with real (not contract-only) Layer 2 backend verification done.** Only remaining gap to run it end-to-end in a Simulator is the same pre-existing blocker as every prior bolt: **`pod install` on the mobile side** — still blocked by the Ruby/bundler toolchain issue (see below). The `backend/` server must be running (`cd backend && npm run dev`) and the `pools` remote's own dev server (port 8083) must be started whenever the mobile app is used to reach the Pools screen.
- **Bolt 8 (Pools advanced + predictions↔pools integration + account deletion) is next per the bolt plan** — both its dependencies (Bolt 6, Bolt 7) are now done. Its scope: POOLS-3 (directed invites), POOLS-6 (predictions grid + anti-bias masking — must be verified at the API-response level per its own story's AC, not just the UI), POOLS-7 (ownership transfer), PREDICTIONS-3 (pool override + dual-save), PREDICTIONS-4 (reset override), AUTH-6 (account deletion, sequenced here because it needs POOLS-7's transfer capability first). Risk: Medium-High per bolt-plan.md — masking bugs are privacy regressions, dual-save/account-deletion are all-or-nothing data-integrity flows; test failure paths, not just happy paths. **Bolt 7's own MF-placement ADR (ADR-032) should be re-examined at Bolt 8's Design stage** if the predictions↔pools integration turns out to need more than the narrow picker-list pull this bolt anticipated — see ADR-032's consequences section for the explicit "revisit if new evidence" escape hatch. Confirm with the user to start Bolt 8.
- **ADR-030 (2026-07-01): backend work now folds into each bolt, not a separate phase track.** Bolt 7 is the second bolt built under this rule (after being introduced specifically because of Bolt 7's `pools.*` gap) — both mobile and backend capabilities shipped in the same Implement/Test stage, verified together for real. Bolt 8 should follow the same discipline for its own new capabilities (directed invites, ownership transfer, dual-save, account deletion).
