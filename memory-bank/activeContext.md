# Active Context

> **Agent note:** This is your short-term memory. Read it at the start of every session and update it immediately after making an important decision, changing focus, or encountering a blocker.

## Current Focus
- Inception (intent `liga-mundial-mobile-migration`) is fully approved (Checkpoints 1–4). **Construction's Bolt 0 (platform scaffolding) is closed** — Model/Design/ADR/Implement/Test all done, including a real iOS Simulator run — see `memory-bank/bolts/bolt-0-platform-scaffolding/implement-and-test.md`. **Bolt 1 (Auth core) is next, to be started in a separate session.**
- The app is no longer the bare RN CLI starter screen: `App.tsx` now renders `AppProviders` (Gesture Handler + SafeArea + TanStack Query) → `RootNavigator` (React Navigation native-stack) → a placeholder `HomeScreen` with a button that loads the federated `education` remote on demand.
- Module Federation is live and confirmed working end-to-end on device: `rspack.config.mjs` (host) + `rspack.config.education-remote.mjs` (one example remote) + `src/host/script-manager-setup.ts` (runtime resolver). `yarn start` runs the host, `yarn start:education` runs the remote dev server on port 8082 — both must be running simultaneously for the remote-loading button to work at runtime (confirmed on iOS Simulator). Still open, non-blocking: the fallback path (remote dev server down) and an Android run haven't been exercised yet.

## Recent Technical Decisions
- **Inception-level decisions** (binding, `requirements.md §7`): backend architecture = Option A; single Supabase-access adapter mandatory; no code dependency on `betmeet-clone` (`scoring` reimplemented in this repo); Module Federation placement per feature (auth/profile/**predictions**/notifications-permission-logic → host; pools/competition/scoring-rankings/notifications-preferences/education/admin → remote; `scoring` → shared library); TOTP-only MFA at launch; FCM+APNs direct; `react-native-keychain`/`react-native-image-picker`/`react-native-svg`/`betmeet://`+Universal/App Links required; iOS+Android only, no offline support.
- **Bolt 0 ADRs (Construction-level, now implemented)** — see `memory-bank/bolts/bolt-0-platform-scaffolding/adr-*.md`: layered folders `domain/platform/shared/host/remotes` (ADR-001); host = root app + one example remote `education` scaffolded now, others repeat the pattern in their own bolts (ADR-002); **navigation = React Navigation (native-stack)** (ADR-003); **state = TanStack Query (server) + Zustand (client, not yet consumed — first consumer is unit-01-auth)** (ADR-004); `scoring` ships as a shared-singleton module under `src/shared/scoring/`, not a workspace package, since this repo has no workspaces (ADR-005); `@/*` → `src/*` alias, wired in `tsconfig.json` + both Rspack configs + Jest (ADR-006).
- Still open, deferred further (per requirements.md §8 and ADR notes): concrete performance budget numbers; push-SDK choice (Expo Notifications vs. direct FCM/APNs SDKs — needed before Bolt 10); exact `BACKEND_API_BASE_URL`/`SUPABASE_URL`/`SUPABASE_ANON_KEY` env-var injection mechanism (stubbed with a clear throw in `src/platform/*/config.ts` until unit-01-auth wires it for real).

## Known Issues / Blockers
- ~~Both `yarn.lock` and `package-lock.json` were present~~ — **resolved in Bolt 0**: `package-lock.json` removed, Yarn is now the sole package manager (Node 22.x required to match `engines`; this environment's default `nvm` shell was on Node 20, used `nvm use 22.20.0` for all Bolt 0 commands — re-confirm your shell's default before the next bolt).
- `metro.config.js` still exists but is now provably dead — `rspack.config.mjs` is the real, MF-enabled host config; safe to delete in a future cleanup bolt, not done here to keep Bolt 0's diff focused.
- iOS bundle identifier is still the RN CLI placeholder — needs updating before any release build.
- `ios/BetmeetMobile.xcworkspace/`, `ios/Podfile.lock`, and `Gemfile.lock` are untracked — still unresolved, unrelated to Bolt 0.
- Repo's HEAD is **detached** at the initial commit (not on a branch) — pre-existing, not caused by or fixed in this bolt; flagging since the next `git commit` would not land on any branch until this is addressed.
- Layer 2 (device-level) verification ran on a real iOS Simulator and is now green for the happy path. Hit two real bugs along the way, both fixed — see `memory-bank/bolts/bolt-0-platform-scaffolding/implement-and-test.md` for the full root-cause writeup: (1) `@module-federation/enhanced` was pinned too new (`^2.6.0` vs. Re.Pack 5.2.5's own tested `0.8.9`); (2) `@react-navigation/native`/`native-stack`'s ESM subpath ships a version-less `package.json`, breaking Module Federation's shared-version auto-detection (`RUNTIME-006`) — fixed by resolving each shared dep's real `version` explicitly in both Rspack configs.
- Project folder was moved on disk (now under `dynamicdevs/proyectos/kinela/` instead of `dynamicdevs/futbol/`). JS/TS side (Jest, typecheck, lint, both Rspack dev servers) was unaffected — all relative. `pod install` was rerun and most of `ios/Pods` now points at the new path; **one stale reference remains**: `HERMES_CLI_PATH` in `Pods-BetmeetMobile.debug.xcconfig` still points at the old `dynamicdevs/futbol/...` path. Doesn't block Simulator/dev-server runs (confirmed), but would break a Hermes-bundling build step (e.g. a Release build) — rerun `cd ios && bundle exec pod install` (or `pod install --repo-update`) if that's hit.
- FCM/APNs project setup needed before Bolt 10. AUTH-6 is sequenced into Bolt 8 (needs `unit-06-pools`'s ownership-transfer first) — don't pull it forward alone.

## Immediate Next Step
- **Bolt 0 is closed.** Bolt 1 (Auth core: sign-in/up, navigation guard, secure session storage) is next per `bolt-plan.md`'s sequencing — to be kicked off with `/bolt-start` in a separate session.
