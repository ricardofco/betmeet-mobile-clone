# CLAUDE.md

> Project memory loaded every session. Generated by the rn-repack-aidlc plugin.
> Fill the `[PROJECT]` blanks once; keep this file in sync with `memory-bank/standards/`.

## Project
- **Name:** `BetmeetMobile`
- **What it is:** `React Native app bootstrapped with React Native 0.86 + Re.Pack (Rspack); feature scope TBD beyond the starter screen.`
- **Platforms:** `iOS, Android`

## Stack (authoritative — see memory-bank/standards/tech-stack.md)
- **Framework:** React Native 0.86.0 (New Architecture enabled).
- **Bundler:** **Re.Pack** 5.2.5 (Rspack 1.7.8 + Module Federation v2) — **NOT Metro**. Never generate Metro config. Module Federation is not yet scaffolded — run `/repack-init`.
- **Language:** TypeScript 5.8 (strict).
- **Lists:** FlashList (not yet installed). **Navigation:** native navigators (not yet installed). **State:** `not yet chosen`.
- **Federation:** host + on-demand remote chunks — boundaries not yet defined. See `memory-bank/standards/system-architecture.md`.

## Session start (read first)
- Read `memory-bank/activeContext.md` (current focus, recent decisions, blockers, next step) and `memory-bank/progress.md` (milestones, bolt statuses) to restore where the work left off.
- Update both as you make decisions, change focus, or finish a stage/bolt — they are the project's short- and long-term memory across sessions.

## How to work here (AI-DLC)
- Pick a flow by size: `/simple-spec` (small), `/fire` (medium, brownfield), AI-DLC `/aidlc-inception` → `/bolt-start` → `/operations` (large/new remote). Unsure → `/spec-flow`.
- All work traces to a story → unit → intent under `memory-bank/`.

## Skill precedence (project wins)
1. If an installed skill contradicts this file or `memory-bank/standards/`, **this project wins**.
2. If a skill complements it (e.g. an FPS optimization), follow it.
3. If a skill's advice breaks an architectural rule (federation boundaries, native-module placement), **don't apply it silently** — raise it.

## Performance skills — when to use which
- Writing RN components → `vercel-react-native-skills`.
- Debugging a measured perf problem → `react-native-best-practices`.
- React-general logic (data fetching, re-renders) → `vercel-react-best-practices`.
- Reusable component design → `vercel-composition-patterns`.

## Testing
- Components → React Native Testing Library (not yet a dependency; add when writing the first real component test). Device E2E / federated-chunk loading → `agent-device`.

## Known gaps (see memory-bank/activeContext.md for detail)
- Dual lockfiles present (`yarn.lock` + `package-lock.json`) — resolve before adding dependencies.
- `metro.config.js` is legacy scaffold, not the active bundler.
- iOS bundle identifier is still the RN CLI placeholder.
